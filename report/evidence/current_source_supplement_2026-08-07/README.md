# Current-source supplement

The integrated campaign records Git HEAD
`c44c501640e3dab35ab241384c99963e1b9d8745`, a binary worktree patch and a
483-file source manifest. Git patches do not contain untracked files. The two
untracked runtime-source files named by the campaign's `git.status_porcelain`
record are therefore preserved here under their repository-relative paths.

Together, the recorded HEAD, the campaign's
`source/worktree.patch`, these two files and the campaign's
`source/source_files.json` provide the material needed to reconstruct and
verify the exact runtime source tree identified by aggregate SHA-256
`326ccd537a65c43bca5780610d4fb9a0d7bd95791b047f3a1e174fc481f889f9`.
