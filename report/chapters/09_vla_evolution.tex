\chapter[AI Inputs: English and Visual-State Learning]
{Developing the AI Inputs: English Understanding and Visual-State Learning}
\label{chap:ai-development}

\section{Phase~2 objective and implementation sequence}

Phase~2 added two AI inputs to Room~315: English requests become confirmed
\code{TaskGoal} records, while paired cameras produce checked
\code{VisualStateObservation} records for planning shuttle motion. The latter
is a versioned paired-frame record carrying fixed-identity estimates, image
timestamps, schema/checkpoint identity, validation status and provenance
(producer and source). These typed records, not free text or raw images, form
the planning boundary.

\section{Reused components and project-specific development}

The language model was integrated pretrained and quantised, without Room~315
fine-tuning; the visual network reused pretrained features and was then trained
on the project's paired-camera corpus.

\begin{table}[htbp]
\centering
\small
\caption{Reused foundations and project-specific Phase~2 work.}
\label{tab:reused-developed-ai}
\begin{tabularx}{\textwidth}{@{}p{0.24\textwidth}p{0.27\textwidth}X@{}}
\toprule
\textbf{Component} & \textbf{Reused foundation} & \textbf{Developed in this
project}\\
\midrule
English understanding & Pretrained Qwen2.5-1.5B-Instruct weights and the local
\code{llama.cpp} inference backend & Saved local configuration, constrained
prompt, strict JSON schema, explicit-fact extraction, field fusion,
clarification, validation and confirmation dialogue\\
\tablerowrule
Visual state & ImageNet-pretrained TorchVision ResNet-18 & Gazebo corpus,
rail-isolated paired-view architecture, 168 learned values, masked multi-head
loss, calibrated segment confidence, grouped evaluation and hash-bound runtime
integration\\
\tablerowrule
Planning & PlanSys2 and its POPF solver & Room~315 PDDL model, fresh-problem
generation and closed-loop integration (\cref{chap:neurosymbolic})\\
\bottomrule
\end{tabularx}
\end{table}

\section{Building the English task interface}

\subsection{Local model selection and configuration}

The selected semantic model is
\code{Qwen2.5-1.5B-Instruct-GGUF} \citep{qwen25,qwen25gguf}, specifically
\code{qwen2.5-1.5b-instruct-q4\_k\_m.gguf}: GGUF is the local file format and
Q4\_K\_M its four-bit quantisation. It runs offline on the CPU through
\code{llama.cpp} \citep{llamacpp}, with a 4,096-token context, temperature~0,
192 generated tokens at most and seed~315; its hash is in
\cref{app:reproducibility}.

Without fine-tuning, a versioned prompt constrains the model to one
\code{SemanticParseEnvelope} JSON object over permitted task fields,
identities, rails, stations and slots; it excludes PDDL, plan steps and device
or shuttle commands.

\subsection{From model output to a confirmed task}

A rule-based extractor records explicit facts (for example, shuttle colour,
rail or slot), Qwen proposes intent fields, and fixed-precedence fusion prevents
the proposal from overwriting explicit or confirmed context. The resulting
non-executable \code{TaskGoalDraft} is clarified, domain-validated and confirmed
before an immutable \code{TaskGoal} is issued.

For the indirect request ``Could you get a loaded shuttle over to the right
slot~4?'', the saved record shows the model supplying
\code{goal\_type=transport} and deterministic fusion/validation constraining
side, payload and destination. The ten-case evaluation is reported in
\cref{sec:language-contract-evidence}.

\section{Generating the Room~315 visual corpus}

\subsection{Paired-camera corpus and partitioning}

The generator enumerated all $2^8-1=255$ non-empty subsets of identities
L1--L4/R1--R4 and produced eight geometrically varied scenes per subset by
changing valid rail positions, payload states and switches. After each Gazebo
scene settled, it captured one synchronised image per overhead camera: 2,040
paired observations, or 4,080 RGB files. \Cref{fig:paired-camera-training}
shows one input.

Count units are: \emph{presence configuration}, the subset declared present;
\emph{scene}/\emph{scenario}/\emph{record}, one labelled paired-camera row;
\emph{image}, one camera file (normally two per row); and
\emph{visible-shuttle target}, one present identity with complete supervision,
including a valid projected box (the per-shuttle metric denominator).

\begin{figure}[htbp]
  \centering
  \begin{subfigure}[t]{0.485\textwidth}
    \includegraphics[width=\textwidth]{f13_left_raw.png}
    \caption{Left-camera RGB input}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.485\textwidth}
    \includegraphics[width=\textwidth]{f13_right_raw.png}
    \caption{Right-camera RGB input}
  \end{subfigure}
  \caption[Paired Room~315 camera observation used for visual training.]
  {The two synchronised RGB images form the complete learned input for one
  scenario. Simulator state is used to construct labels in a separate file and
  is never appended to the image tensor.}
  \label{fig:paired-camera-training}
\end{figure}

Gazebo identity, rail coordinate, payload state and projected box supply labels
stored separately from image references. Only paired RGB images are model
features; depth, controller state, point clouds and other oracle metadata remain
label-generation data.

\paragraph{Historical grouped protocol.}
All eight variants of each presence configuration stayed together. The
predecessor split assigned 191 configurations/1,528 scenarios to training and
32/256 each to Grouped Validation and a held-out Test. Grouped Validation
images/labels were evaluated every epoch; their weighted task-loss sum selected
epoch~14 and drove patience, though all 15 epochs ran. The Test stayed outside
training/selection, then was unlocked and evaluated once on 30 July 2026 after
selection, supplying the predecessor acceptance record rather than the current
claim. It is therefore a consumed historical Test, not a still-unseen ``locked
partition'', and current loaders denylist its recorded paths and hashes.
Neither 256-scenario partition directly enters V4. Grouped Validation has only
indirect lineage: its epoch-14 V3 joint-view checkpoint initialised the V4
rail-isolated backbone, but its heads were not copied. Both partitions remain
only as historical reproducibility and audit records.

\subsection{Current training, validation and Canary roles}

The current pool combines 1,528 legacy grouped-training scenes with 4,000
training-only hard cases. Each deterministic epoch samples 4,000 weighted records with replacement
from each source; weights favour rare side--segment cells (rail side paired with
public segment), boundaries, switch zones and multi-blocker scenes. A
\emph{hard case} deliberately stresses such factors; it was generated, not
mined from Final Test errors.

The separate hard-case Validation (512 scenes; 2,254 present targets) selected
V4, drove patience and supplied temperature/segment-threshold fitting. Only
after selection, the Development Canary (256 scenes; 1,085 targets) checked the
frozen candidate for degradation; it affected neither selection nor calibration
and is not Test evidence. Accessing the current Final Test remained prohibited
throughout development; it supplies the principal evidence below.

\section{Designing and training the visual-state estimator}

The \emph{visual model} is the learned network; the \emph{visual-inference
runtime} adds preprocessing and observation validation; the \emph{closed-loop
runtime} further adds the task gateway, planner, supervisor and controllers.

\paragraph{Evolution of the visual pipeline.}
The early versions primarily evolved the data contract: V1 established
paired-camera inputs while keeping simulator-derived labels outside the model
input, and V2 added continuous along-rail localisation. V3 fixed the
representation to the eight known shuttle identities, resized both cameras to
$224\times224$, concatenated global features from a shared ImageNet-pretrained
ResNet-18 and predicted 200 values through one head. This design distorted the
native $4{:}3$ images, allowed cross-rail influence and learned side, $s_m$ and
segment length even though identity and topology determine them. V4 addresses
these limitations with $320\times240$ inputs, weights shared only through
\code{layer3}, independent \code{layer4}/spatial-query branches for each rail
and 168 learned values, while deriving topology-known quantities
deterministically. V4 is the active visual model used by the evaluated runtime.
Results are in \cref{sec:visual-v4-results}.

\subsection{Rail-isolated 168-value learned contract}

Each $4{:}3$ image is resized to $320\times240$, ImageNet-normalised and
processed separately by shared ResNet-18 weights through \code{layer3}. Each
rail then has its own \code{layer4} and four identity-query heads; no camera
feature enters the other rail's late branch.

The fixed identity order remains L1--L4 followed by R1--R4. Each identity has
21 learned values:

\begin{equation*}
  21 =
  \underbrace{14}_{\text{segment logits}} +
  \underbrace{2}_{\text{loaded-state logits}} +
  \underbrace{4}_{\text{bounding box }(x,y,w,h)} +
  \underbrace{1}_{s_{ratio}},
  \qquad 8\times21=168 .
\end{equation*}

A logit is an unnormalised pre-softmax score; a box is normalised top-left
$x,y$, width and height. The learned coordinate is that of \cref{eq:sratio}:
\(
  s_{ratio}\equiv\rho=s_m/L_{segment}\in[0,1]
\), where $s_m$ is the within-segment arc length in metres and
$L_{segment}$ is that segment's length.

Identity determines side (L1--L4 left; R1--R4 right); after decoding the
segment, the runtime reads its fingerprinted topology length and computes
$s_m=s_{ratio}L_{segment}$. A derived 200-value compatibility vector is
diagnostic only and excluded from control.

\begin{figure}[p]
  \centering
  \input{figures/f12_visual_model_contract_v2.tex}
  \caption[Rail-isolated visual model and deterministic state derivation.]
  {The model shares low-level ResNet-18 weights through \code{layer3}, then keeps
  \code{layer4} and the four-query prediction head independent for each rail.
  The model produces 168 learned values; side, segment length and $s_m$ are
  derived deterministically before whole-observation validation.}
  \label{fig:visual-model-contract}
\end{figure}

Presence is an independent input, not a prediction. Training masks any identity
that is absent or lacks a required target. At runtime, every present identity
must pass artefact, camera-order, freshness, vocabulary, numeric and confidence
checks; any failure rejects the whole observation rather than publishing a
partial state.

\subsection{Training and checkpoint selection}

Verified predecessor weights initialised the shared stem through \code{layer3}
and each independent \code{layer4}; no joint head transferred, and rail-branch
parameters, BatchNorm buffers and query heads were optimised independently.

Training unfroze heads in epoch~1, independent \code{layer4} branches in
epoch~2 and shared \code{layer3} at a lower rate in epoch~4. The objective
weighted segment cross-entropy 4, $s_{ratio}$ Smooth-L1 2, loaded-state
cross-entropy 1, box L1/generalised-IoU 1 and graph-distance topology loss 0.25.
Brightness, contrast, saturation, gamma, blur and noise augmentations were
allowed; side-breaking flips and camera swaps were forbidden. Exact ranges and
the selected configuration follow.

\begin{table}[htbp]
\centering
\small
\caption{Configuration of the selected visual training run.}
\label{tab:visual-training-config}
\begin{tabularx}{\textwidth}{@{}p{0.29\textwidth}X@{}}
\toprule
\textbf{Item} & \textbf{Configured value}\\
\midrule
Input & Fixed left--right RGB order; aspect-preserving bilinear resize to
$320\times240$; ImageNet normalisation\\
\tablerowrule
Architecture & Shared ResNet-18 through \code{layer3}; independent
\code{layer4} and four-query spatial head per rail; no cross-camera feature path\\
\tablerowrule
Optimiser & AdamW; head, \code{layer4} and shared-\code{layer3} learning rates
$3\times10^{-4}$, $10^{-4}$ and $2\times10^{-5}$; weight decay $10^{-4}$\\
\tablerowrule
Batch and schedule & Batch size 24; 12 epochs; mixed precision; staged
unfreezing; early-stopping patience 4\\
\tablerowrule
Loss & Segment 4; $s_{ratio}$ 2; loaded state 1; bounding box 1; topology 0.25;
presence-aware masks\\
\tablerowrule
Selection & Lexicographic validation planning score, worst side--segment
recall, then correct-segment $s_m$ 95th percentile (p95)\\
\tablerowrule
Reproducibility & CUDA training, seed 31520260811; Final Test unavailable and
forbidden during training, selection, calibration and threshold selection;
Canary excluded from training and selection\\
\bottomrule
\end{tabularx}
\end{table}

All 12 epochs completed and epoch~11 was frozen (identity in
\cref{app:reproducibility}). Here, an epoch is 8,000 sampled records, not one
unique pass through the 5,528-scene pool; a checkpoint is the saved weight
state after an epoch. Comparison was lexicographic: first the weighted planning
score
\(0.30F_{1,\mathrm{macro}}+0.15A_{\mathrm{min\,side}}+0.20R_{\mathrm{worst\,cell}}
+0.20A_{\mathrm{joint},0.05}+0.075A_{\mathrm{switch}}+0.075A_{\mathrm{boundary}}\)
(segment macro F1, lower-side top-1, worst-cell recall, joint
segment/coordinate accuracy at \(\lvert\Delta s_{ratio}\rvert\leq0.05\), and
switch-/boundary-zone top-1); then worst side--segment recall; then the lowest
correct-segment $s_m$ p95. Four unimproved epochs would stop training; this
patience~4 condition was not reached.

Hard-case Validation fitted the segment temperature
0.6007370031399095 (dividing logits before softmax) and then the deployed score
floor 0.9139089813389109 from its retention--accuracy curve. Loaded state uses
an uncalibrated maximum-softmax floor of 0.5, not a probability guarantee. The
runtime binds checkpoint hash, architecture, preprocessing, camera order and
segment-length fingerprint before becoming ready; \cref{sec:visual-v4-results}
distinguishes these online floors from labelled offline tolerances.

\subsection{Preregistered Final Test}

Before evaluation, the checkpoint, scenario plan, evaluator, coverage,
thresholds and single-run policy were frozen. The frozen Final Test comprised
1,040 scenes, 2,080 images and 4,680 visible targets, and was executed once
after model selection. \emph{Preregistered} here means the
repository controls were frozen before any Test prediction, not registered
externally. One normalised control schema and hashes then established
byte-identical captured data before the attempt lock.

Independence/disjointness covers procedure, scene IDs, canonical
generation-parameter fingerprints, image bytes and complete label rows, not
presence configurations; abstract task factors intentionally recur.

After these checks, a single-use attempt lock and completion ledger were created
and the checkpoint ran once, without training, selection, calibration refit or
threshold selection. This evaluation lock is unrelated to rail-route
reservation. Results, development-partition metrics and synthetic-camera limits
are consolidated in \cref{sec:visual-v4-results}.

\section{Integration outputs and runtime qualification}

Integration exposes only a confirmed \code{TaskGoal} and current
\code{VisualStateObservation}, carrying the scores, schema and checkpoint hash
required by the gateway. \Cref{chap:neurosymbolic} follows their conversion to
planning facts; \cref{sec:visual-v4-results} defines qualification and authority
lifecycle. In particular, node-local \code{dry\_run\_state\_fusion} blocks only
the redundant ProblemExpert mirror: the separately activated gateway builds
PDDL problems and owns Gazebo-only actuation.
